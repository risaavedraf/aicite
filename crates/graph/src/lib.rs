/// Graph manages document/section/chunk relationships
pub struct Graph;

impl Graph {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Graph {
    fn default() -> Self {
        Self::new()
    }
}
