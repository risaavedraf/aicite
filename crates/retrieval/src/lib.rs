/// Retrieval performs vector-first semantic search
pub struct Retrieval;

impl Retrieval {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Retrieval {
    fn default() -> Self {
        Self::new()
    }
}
